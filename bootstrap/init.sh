#!/bin/sh
cat ../../.bashrc > ~/.bashrc
cat ../../.bashrc.local > ~/.bashrc.local

cat ../../.gitconfig > ~/.gitconfig
cat ../../.gitconfig.local > ~/.gitconfig.local
cat ../../.gitignore.global > ~/.gitignore.global
